# Contributing

Use a short-lived branch and a focused pull request. Run `npm run check` and build the Docker image before requesting review. Never commit credentials, customer data, WhatsApp exports, receipts, or production logs.

Architectural changes should preserve tenant isolation, explicit approval for consequential actions, auditable evidence, and safe simulation defaults.
